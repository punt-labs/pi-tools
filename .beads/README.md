# Beads (pi-tools)

Issue tracking uses the Punt Labs central Hosted DoltDB instance. Issues for
this repo use the `pit` prefix and `repo:pit` label.

Run Beads from the Nix dev shell so `bd` comes from the repo toolchain.
